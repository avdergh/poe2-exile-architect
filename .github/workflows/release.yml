name: Release

# Tag a release (e.g. `git tag v0.1.0 && git push --tags`) to build per-OS self-contained
# .mcpb bundles and publish the self-update assets (corpus + engine + update-manifest).
on:
  push:
    tags: ["v*", "data-*"]

permissions:
  contents: write

env:
  POB_COMMIT: 860f4268299739ce9df87c4f373abe35824101cf # pinned PoB-PoE2 (see pob/PINNED.md); CI checks out this exact SHA
  LUAJIT_COMMIT: 871db2c84ecefd70a850e03a6c340214a81739f0 # matches PoB's pinned LuaJIT

jobs:
  bundles:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, plat: linux-x64 }
          - { os: macos-latest, plat: mac-arm64 }
          - { os: windows-latest, plat: win-x64 }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv python install 3.12

      - name: Build LuaJIT (Unix)
        if: runner.os != 'Windows'
        run: |
          export MACOSX_DEPLOYMENT_TARGET=11.0  # required by LuaJIT on macOS; ignored on Linux
          git clone https://github.com/LuaJIT/LuaJIT luajit-src
          cd luajit-src && git checkout "$LUAJIT_COMMIT" && make
          mkdir -p "$GITHUB_WORKSPACE/runtime/luajit/${{ matrix.plat }}"
          cp src/luajit "$GITHUB_WORKSPACE/runtime/luajit/${{ matrix.plat }}/luajit"

      - name: Set up MSYS2 (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          install: git make mingw-w64-ucrt-x86_64-gcc

      - name: Build LuaJIT (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: |
          git clone https://github.com/LuaJIT/LuaJIT luajit-src
          cd luajit-src && git checkout "$LUAJIT_COMMIT" && make
          dest="$(cygpath -u "$GITHUB_WORKSPACE")/runtime/luajit/win-x64"
          mkdir -p "$dest"
          # luajit.exe links lua51.dll; its only other deps are the Windows UCRT (system DLLs).
          cp src/luajit.exe src/lua51.dll "$dest/"

      - name: Check out pinned PoB + apply fork patches
        shell: bash # Windows runners default run: to pwsh, which won't glob ../patches/*.patch
        run: |
          git clone --filter=blob:none --no-checkout https://github.com/PathOfBuildingCommunity/PathOfBuilding-PoE2.git pob/PathOfBuilding-PoE2
          git -C pob/PathOfBuilding-PoE2 config core.autocrlf false # keep LF so the LF patch applies on Windows
          git -C pob/PathOfBuilding-PoE2 checkout -q ${{ env.POB_COMMIT }}
          (cd pob/PathOfBuilding-PoE2 && git apply --ignore-whitespace ../patches/*.patch)

      - run: uv sync
      - run: uv run ruff check server scripts pipeline tests
      - name: Format advisory
        continue-on-error: true
        run: uv run ruff format --check server scripts pipeline tests
      - run: npx --yes @anthropic-ai/mcpb validate manifest.json
      - run: uv run python -m pipeline.build_corpus
      - name: Full non-compute release gate
        shell: pwsh
        run: ./scripts/verify.ps1 -Profile full

      - name: Build bundle
        run: uv run python scripts/build_bundle.py --version ${{ github.ref_name }} --platform ${{ matrix.plat }}

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/poe2-build-mcp-${{ github.ref_name }}-${{ matrix.plat }}.mcpb

  update-assets:
    # Publishes the self-update payload (engine + corpus) and the manifest update.py reads.
    needs: bundles
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: sudo apt-get update && sudo apt-get install -y luajit zip
      - name: Check out pinned PoB + apply fork patches
        shell: bash # Windows runners default run: to pwsh, which won't glob ../patches/*.patch
        run: |
          git clone --filter=blob:none --no-checkout https://github.com/PathOfBuildingCommunity/PathOfBuilding-PoE2.git pob/PathOfBuilding-PoE2
          git -C pob/PathOfBuilding-PoE2 config core.autocrlf false # keep LF so the LF patch applies on Windows
          git -C pob/PathOfBuilding-PoE2 checkout -q ${{ env.POB_COMMIT }}
          (cd pob/PathOfBuilding-PoE2 && git apply --ignore-whitespace ../patches/*.patch)
      - run: uv sync && uv run python -m pipeline.build_corpus

      - name: Assemble update payload + manifest
        run: |
          mkdir -p rel
          cp data/corpus.sqlite rel/corpus.sqlite
          zip -r rel/pob-engine.zip \
            pob/pob_headless.lua \
            pob/PathOfBuilding-PoE2/src \
            pob/PathOfBuilding-PoE2/runtime/lua \
            -x '*.zst' '*.dds' '*.png' '*.jpg' '*.jpeg' '*.tga' '*.gif' '*.bk2' '*.mp4' '*.ogg' '*.mp3'
          POB_COMMIT=$(git -C pob/PathOfBuilding-PoE2 rev-parse HEAD)
          BASE="https://github.com/${{ github.repository }}/releases/latest/download"
          # source_sha = hash of the deterministic upstream inputs (RePoE + wiki), so the scheduled
          # refresh can detect "did upstream change?" without false positives from build timestamps.
          SRC_SHA=$(python - <<'PY'
          import hashlib, glob, os
          h = hashlib.sha256()
          for p in sorted(glob.glob("data/raw/*.json")) + sorted(glob.glob("data/raw/wiki/*.json")):
              h.update(os.path.basename(p).encode()); h.update(open(p,"rb").read())
          print(h.hexdigest())
          PY
          )
          python - "$POB_COMMIT" "$BASE" "$SRC_SHA" <<'PY'
          import hashlib, json, sys
          from server import paths
          commit, base, src_sha = sys.argv[1], sys.argv[2], sys.argv[3]
          def sha(p): return hashlib.sha256(open(p,"rb").read()).hexdigest()
          compat = json.load(open("data/compatibility/pob.json", encoding="utf-8"))
          # Freshness gates must come from the certification manifest, never from release names.
          certified = next((e for e in compat.get("entries", []) if e.get("commit") == commit), None)
          if certified is None:
              raise SystemExit(f"PoB commit {commit} is not certified in data/compatibility/pob.json")
          tag = "${{ github.ref_name }}"
          manifest = {
              "version": tag,                       # data version (apply_updates compares this)
              "app_version": tag.lstrip("v"),       # .mcpb/code version (decoupled .mcpb nag)
              "engine_contract": paths.POB_RUNTIME_CONTRACT,
              "pob_commit": commit,
              "pob_version": certified["pob_version"],
              "game_patch": certified["game_patch"],
              "passive_tree": certified["passive_tree"],
              "source_sha": src_sha,
              "corpus": {"url": f"{base}/corpus.sqlite", "sha256": sha("rel/corpus.sqlite")},
              "engine": {"url": f"{base}/pob-engine.zip", "sha256": sha("rel/pob-engine.zip")},
          }
          json.dump(manifest, open("rel/update-manifest.json","w"), indent=2)
          print(json.dumps(manifest, indent=2))
          PY

      - uses: softprops/action-gh-release@v2
        with:
          files: |
            rel/corpus.sqlite
            rel/pob-engine.zip
            rel/update-manifest.json
