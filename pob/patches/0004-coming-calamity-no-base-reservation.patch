diff --git a/src/Classes/Item.lua b/src/Classes/Item.lua
index f65e065..a97cfff 100644
--- a/src/Classes/Item.lua
+++ b/src/Classes/Item.lua
@@ -2841,14 +2841,35 @@ function ItemClass:BuildModList()
 			end
 		end
 	end
+	local grantedSkillList = baseList:List(nil, "ExtraSkill")
+	local isComingCalamity = self.rarity == "UNIQUE"
+		and self.title == "The Coming Calamity"
+		and self.baseName == "Heroic Armour"
+	if isComingCalamity then
+		local grantedIds = { }
+		for _, skill in ipairs(grantedSkillList) do
+			grantedIds[skill.skillId] = true
+		end
+		isComingCalamity = grantedIds.HeraldOfAshPlayer
+			and grantedIds.HeraldOfIcePlayer
+			and grantedIds.HeraldOfThunderPlayer
+	end
 	self.grantedSkills = { }
-	for _, skill in ipairs(baseList:List(nil, "ExtraSkill")) do
+	for _, skill in ipairs(grantedSkillList) do
 		if skill.name ~= "Unknown" then
 			t_insert(self.grantedSkills, {
 				skillId = skill.skillId,
 				level = skill.level,
 				noSupports = skill.noSupports,
 				noReservation = self.base and self.base.grantedSkillsHaveNoReservation or nil,
+				-- The Coming Calamity grants its three Heralds without their base
+				-- reservation. This is an item-specific rule, not a generic rule for
+				-- item-granted skills.
+				noSpiritReservation = isComingCalamity and (
+					skill.skillId == "HeraldOfAshPlayer"
+					or skill.skillId == "HeraldOfIcePlayer"
+					or skill.skillId == "HeraldOfThunderPlayer"
+				) or nil,
 				source = self.modSource,
 				triggered = skill.triggered,
 				triggerChance = skill.triggerChance,
diff --git a/src/Modules/CalcActiveSkill.lua b/src/Modules/CalcActiveSkill.lua
index 238cf5b..882e0c6 100644
--- a/src/Modules/CalcActiveSkill.lua
+++ b/src/Modules/CalcActiveSkill.lua
@@ -728,6 +728,10 @@ function calcs.buildActiveSkillModList(env, activeSkill)
 				grantedEffectLevel[k] = v
 			end
 		end
+		if activeEffect.srcInstance and activeEffect.srcInstance.noSpiritReservation then
+			grantedEffectLevel.spiritReservationFlat = 0
+			grantedEffectLevel.spiritReservationPercent = 0
+		end
 		activeEffect.grantedEffectLevel = grantedEffectLevel
 		return
 	end
@@ -803,6 +807,10 @@ function calcs.buildActiveSkillModList(env, activeSkill)
 			grantedEffectLevel[k] = v
 		end
 	end
+	if activeEffect.srcInstance and activeEffect.srcInstance.noSpiritReservation then
+		grantedEffectLevel.spiritReservationFlat = 0
+		grantedEffectLevel.spiritReservationPercent = 0
+	end
 	if activeEffect.srcInstance and activeEffect.srcInstance.noReservation then
 		for _, resource in ipairs({ "mana", "life", "spirit" }) do
 			grantedEffectLevel[resource.."ReservationFlat"] = 0
diff --git a/src/Modules/CalcSetup.lua b/src/Modules/CalcSetup.lua
index e6320d8..edb3576 100644
--- a/src/Modules/CalcSetup.lua
+++ b/src/Modules/CalcSetup.lua
@@ -1953,6 +1953,7 @@ function calcs.initEnv(build, mode, override, specEnv)
 				activeGemInstance.noSupports = grantedSkill.noSupports
 				group.noSupports = grantedSkill.noSupports
 				activeGemInstance.noReservation = grantedSkill.noReservation
+				activeGemInstance.noSpiritReservation = grantedSkill.noSpiritReservation
 				activeGemInstance.triggered = grantedSkill.triggered
 				activeGemInstance.triggerChance = grantedSkill.triggerChance
 				group.gemList[1] = activeGemInstance
